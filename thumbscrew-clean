#!/bin/bash

NAME=${1}

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

SAVE=$(dirname $(find $(git rev-parse --show-toplevel) -name ${NAME}))

/usr/bin/osascript ${DIR}/thumbscrew.scpt ${SAVE} ${NAME} 2>/dev/null
cat -
